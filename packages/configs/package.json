{
  "name": "@workspace/configs",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/env.js",
  "types": "./dist/env.d.ts",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "",
    "build": "esbuild --bundle --outfile=./dist/env.js --platform=node --target=node20 --format=esm ./env.ts && tsc --emitDeclarationOnly"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^22.12.0",
    "typescript": "^5"
  },
  "private": true,
  "dependencies": {
    "esbuild": "^0.24.2",
    "zod": "^3.22.4"
  }
}
